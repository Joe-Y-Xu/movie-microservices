apiVersion: apps/v1
kind: Deployment
metadata:
  name: artifact-catalog-deploy
  namespace: default
  labels:
    app: artifact-catalog-service
    version: ${IMAGE_TAG}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: artifact-catalog-service
  template:
    metadata:
      labels:
        app: artifact-catalog-service
        version: ${IMAGE_TAG}
    spec:
      containers:
      - name: artifact-catalog
        image: ${DOCKER_USER}/artifact-catalog-service:${IMAGE_TAG}
        ports:
        - containerPort: 8080
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 10
        readinessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 10